# apiVersion: argoproj.io/v1alpha1
# kind: Application
# metadata:
#   name: pma-application
#   namespace: argocd
#   finalizers:
#     - resources-finalizer.argocd.argoproj.io
# spec:
#   project: default
#   source:
#     # 1. TrueCharts Helm Chart for phpMyAdmin
#     repoURL: https://truecharts.org
#     chart: phpmyadmin
#     targetRevision: 15.0.0 # Check TrueCharts for the latest version
#     helm:
#       values: |
#         # Configuration for TrueCharts version
#         # TrueCharts handles database connections differently
#         db:
#           mysql:
#             enabled: false
#             # You may need to provide the host/port in the way their chart expects
#             host: mysql-application
#             port: 3306
        
#         # Standard env overrides if needed
#         env:
#           PMA_HOST: mysql-application
#           PMA_PORT: "3306"

#   destination:
#     server: https://kubernetes.default.svc
#     namespace: mysql
#   syncPolicy:
#     automated:
#       prune: true
#       selfHeal: true
#     syncOptions:
#       - CreateNamespace=true
#       - ServerSideApply=true